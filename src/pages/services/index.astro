---
import { Cta } from '@/components/sections/cta';
import { OurProcess } from '@/components/sections/our-process';
import { SITE_TITLE, SITE_DESCRIPTION } from '@/consts';
import DefaultLayout from '@/layouts/DefaultLayout.astro';

const pageTitle = 'Services | ' + SITE_TITLE;
const pageDescription = 'Discover our comprehensive design services - from logo design to complete brand identity systems.';
---

<DefaultLayout title={pageTitle} description={pageDescription}>
  <OurProcess />
  <Cta />
</DefaultLayout>
